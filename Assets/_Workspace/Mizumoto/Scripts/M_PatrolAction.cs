using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class M_PatrolAction : MonoBehaviour
{
    [SerializeField, Header("見渡す場所かどうか")]
    bool overlookingFlg = false;
    public bool OverlookingFlg => overlookingFlg;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
