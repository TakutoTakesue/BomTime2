using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class M_PatrolPosAction : MonoBehaviour
{
    [SerializeField, Header("見渡すポジションかどうか")]
    bool overlookingFlg = true;
    public bool OverlookingFlg => overlookingFlg;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
